<!DOCTYPE html>
    <html>
    <head>
        <title>Simple CMS: <%= @page_title || "Admin Area" %> </title>
    </head>
    <body>

    <% if !flash[:notice].blank? %>
        <div class>
            <%= flash[:notice] %>
        </div>
    <% end %>

    <!-- Before yield -->
        <%= yield %>
    <!-- After yield -->

    </body>
</html>
